import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: process.env.ARK_API_KEY,
  baseURL: 'https://ark.cn-beijing.volces.com/api/v3',
});

const PROMPTS = {
  geo: `你是一位资深的 GEO（Generative Engine Optimization，生成式引擎优化）策略专家，拥有 15 年广告营销经验，专注 AI 营销代运营，核心能力是为品牌打造「AI 时代的数字信任资产」。

请根据提供的品牌信息，生成一份完整的「AI 时代品牌数字信任资产」策略方案，涵盖 GEO 内容策略 + 账号矩阵 + 权威信源 + 行业权威 + 舆情监控五大模块。

【品牌信息】：
{{BRAND_INFO}}

首先提取以下 9 项品牌关键信息。如果输入中缺少某项，基于已有信息合理推断，并在方案开头标注「以下信息由 AI 推断，请确认：」列出推断项。

必须提取的 9 项信息：
1. 品牌名称
2. 行业/品类
3. 核心技术/差异化壁垒（专利、认证、年限等）
4. 产品矩阵（2-5 个核心产品线）
5. 目标人群（C 端和 B 端的核心人群）
6. 品牌权威背书（高新技术企业、认证、出口国家数、服务案例数等）
7. 核心卖点清单（每个产品线的 3-5 个核心卖点）
8. 主要竞品（2-3 个主要竞品名称）
9. 当前营销痛点（品牌当前最想解决的 1-3 个问题）

提取完信息后，直接按下方 13 个部分生成完整方案，不需要再问用户确认。

---

## 方案结构（13 个部分，全部必填，不可省略）

### 第一部分：方案核心定位、目标与底层逻辑

1.1 核心定位
用一段话概括：以 [核心技术] 为锚点，围绕 [产品矩阵]，覆盖 [目标人群]，打造"人-搜索意图-产品-转化"全链路闭环的 GEO 方案，实现从"品类曝光"到"精准种草"再到"全渠道转化"的全域增长。

1.2 核心目标（3 条）
- 抢占 [行业品类] 搜索流量高地，覆盖泛需求、精准需求、痛点需求、品牌需求、对比决策全路径搜索词
- 精准匹配不同人群的搜索意图，强化 [品牌] 的核心优势
- 打通 C 端零售、B 端工程/商业、渠道合作三大转化赛道

1.3 GEO 底层核心逻辑
阐述生成式搜索引擎用户的搜索行为模型。基于该品牌的行业特性、产品决策链路长短、用户认知基础等因素，分析并确定 5 阶段搜索路径中每阶段的流量占比（占比不是固定值，需根据品牌实际情况推导，合计 100%），并为每阶段生成 3-5 个本行业的示例关键词：

| 阶段 | 名称 | 占比 | 用户状态 | 示例关键词 |
|------|------|------|----------|-----------|
| 1 | 问题认知期 | __% | 有问题，不知道解决方案 | |
| 2 | 解决方案探索期 | __% | 在对比不同解决方案 | |
| 3 | 产品对比期 | __% | 决定买某品类，在对比品牌 | |
| 4 | 品牌购买期 | __% | 倾向某品牌，验证是否靠谱 | |
| 5 | 使用问题期 | __% | 已购买，遇到使用问题 | |

---

### 第二部分：GEO 关键词翻译方法论

2.1 核心公式
GEO 关键词 = 用户问题 × 解决方案 × 使用场景

2.2 步骤 1：识别用户痛点（人）
按人群拆解痛点，为每个目标人群列出 5-8 个核心痛点。

2.3 步骤 2：匹配产品卖点（解决方案）
用产品卖点解决用户痛点。

2.4 步骤 3：拓展使用场景（扩展器）
从 3 个维度拓展关键词：时间场景、使用场景、人群场景（C端和B端各给出示例）。

2.5 完整示例
选取一个典型用户，演示从痛点 → 卖点匹配 → 场景拓展 → 关键词生成 → 内容植入的全流程。

2.6 为什么不能直接推荐产品？AI 抓取的底层逻辑
核心论点：AI 看的是文章的整体性，硬广内容不会被抓取。AI 喜欢的文章结构：挖掘痛点 → 展示卖点 → 扩充场景 → 数据/案例证明 → 自然推荐。

---

### 第三部分：产品卖点锚定

3.1 C 端消费人群核心卖点：为每个 C 端产品线列出 5 个核心卖点，包含具体数据。

3.2 B 端商业人群核心卖点：为每个 B 端细分场景列出 5 个专属核心优势（必须包含数据和成熟案例数量）。

---

### 第四部分：传统关键词的问题 + GEO 抓取逻辑解析

4.1 传统关键词的三大问题
1. 关键词太抽象，用户不会这样搜——给出本行业的 ❌ vs ✅ 示例
2. 关键词没有决策焦虑，无法触发转化——给出本行业的 ❌ vs ✅ 示例
3. 关键词是我们的视角，不是用户的视角——给出本行业的 ❌ vs ✅ 示例

4.2 GEO 如何翻译关键词？
三条翻译原则，每条给出 5 个以上本行业实例：
- 极度具体化：从抽象词 → 具体问题
- 植入决策焦虑：从信息词 → 决策词
- 切换到用户视角：从品牌视角 → 用户视角

---

### 第五部分：C 端消费人群全链路 GEO 布局

对每个 C 端目标人群输出：人群画像 & 核心痛点 + 按 5 阶段搜索路径生成 8-15 个关键词/阶段。

---

### 第六部分：B 端商业人群全链路 GEO 布局

对每个 B 端目标人群输出：人群细分 & 核心画像 & 痛点拆解 + 按 5 阶段搜索路径生成 5-10 个关键词/阶段 + 产品匹配 & 内容锚定。

---

### 第七部分：全域关键词矩阵总表

按 4 类汇总所有关键词：
1. 品牌专属词包（高转化精准词）
2. C 端行业品类词包（泛需求流量词）
3. B 端行业品类词包（商业需求流量词）
4. 全人群痛点疑问词包（高意向种草/决策词）

---

### 第八部分：内容生产示例（1 篇完整文章）

选取一个最具代表性的关键词，按以下结构生产一篇完整的 GEO 优化文章：
标题 + 快速回答（100字内结论前置）+ 详细解释（8节）+ FAQ（6-8个）+ 相关问题（5-8个）
全文 3000-5000 字。

---

### 第九部分：GEO 优化落地执行与服务保障

9.1 全链路内容生产与优化（5种搜索意图类型的内容策略）
9.2 流量溢出 + 全平台承接（小红书/视频号/抖音/微信搜一搜关键词埋点计划）
9.3 完整转化闭环

---

### 第十部分：账号矩阵搭建

选择 8 个平台，为每个账号制定定位、主页装修要点、转化路径设计、内容资产沉淀计划（≥100篇）。

---

### 第十一部分：品牌权威信源体系

11.1 品牌百科建设（2个百度百科词条策划）
11.2 权威新闻传播（≥10篇通稿策划）

---

### 第十二部分：重塑品牌行业权威

行业品类调研报告/白皮书策划：报告标题、框架、数据来源、发布渠道。

---

### 第十三部分：全域舆情与竞品监控

13.1 舆情监测体系（覆盖平台、监测关键词、监测机制、负面舆情分级）
13.2 月度舆情分析报告模板
13.3 竞品追踪

---

## 风格与纪律要求

1. 所有关键词必须是用户真实会搜的完整问句，不是抽象词
2. 所有卖点必须带具体数据，不接受模糊表述
3. 所有案例必须有具体人物、城市、产品、数据
4. 品牌植入必须自然，先挖痛点再推产品
5. 对比必须用表格
6. FAQ 必须是用户真实顾虑
7. 全文中文，专业术语可保留英文缩写
8. 每个部分的逻辑必须自洽
9. 方案全文输出为中文 Markdown 格式`,

  'geo-b': `你是一名从事搜索推广优化多年的资深营销策划人，擅长洞察消费者痛点和需求，对购物软件、搜索引擎优化（SEO）、大语言模型搜索优化（GEO）非常熟悉。擅长根据业务需求，对比潜在消费者或客户的需求进行分析，对消费者或客户的搜索词进行拓展、分类。

请根据提供的【产品或服务信息】、【品牌或公司信息】、【核心业务关键词】，撰写一份GEO营销解决方案。

【品牌或公司信息】：{{BRAND_NAME}}
【产品或服务信息】：{{PRODUCT_INFO}}
【核心业务关键词】：{{KEYWORDS}}

你的工作流程：

（一）根据提供的产品或服务信息、品牌或公司信息，分析出一批核心的关键词。
（二）结合行业实际情况，对搜索关键词进行收集、汇总、整理、归类，再进行拓展至80-100个，列出的时候，注意要一行显示一个词。
罗列完每一类关键词，再写一下撰写此类关键词对应的推广文章的结构和要点。

怎么拓展呢？搜索关键词一般按客户需求阶段划分成：需求模糊期、产品对比期、品牌购买期。
1、需求模糊期，一般为业务相关的知识类搜索，客户有需求、但没有表明查找业务相关机构/品牌/厂家/公司的意识，这类词不适合GEO优化，要剔除；
2、产品对比期，对于B端客户来说，会想查找哪家机构/公司/品牌/厂家更匹配自己的需求，一般为：xx推荐、xx哪家好、xx哪家靠谱、xx排名、xx口碑推荐、xx机构名单、xx哪里找、xx哪里有、xx品牌推荐、xx厂家推荐、xx机构推荐、xx公司哪家好等结构，此类为GEO优化的重点；对于C端消费者来说，除了"xxx推荐"、"xxx什么牌子比较好"等常规的搜索词，还有可能搜索场景类搜索关键词，比如"适合某类人群中的xxx"、"适合某场景或节日的xxx推荐"等等；注意：以上搜索词结构仅举例、方便你理解，请勿在拓展搜索词时局限于以上搜索词结构，请根据各行各业情况的人群的搜索习惯进行拓展。
3、品牌购买期，一般品牌相关信息查询（产品服务范围、地址电话等联系方式、服务案例或合作品牌、企业或品牌实力等、是否靠谱/是否有负面新闻等），以疑问句类的信息查询为主，比如：品牌名+怎么样、公司名+怎么联系等等。如果没有提供品牌名，则以文本"品牌/公司名"作为品牌名进行拓词。

搜索关键词怎么归类呢？可以按需求阶段、产品细分类目、场景分类、搜索词结构等分成多个词包，每个词包一般10-30个关键词，本方案中请把行业榜单词（包含排名、榜单、排行榜、top的词）去掉。

（三）输出方案，将上述生成的关键词分类与方案模板中。

方案模板的结构如下：

第一部分，根据提供的业务及品牌信息，对客户的业务需求进行简短分析描述。

第二部分，最关键的内容，即搜索关键词及分类。

第三部分，写上此业务需求如何策划转化链条。这部分主要是从用户需求的阶段开始讲：
1、用户搜索时"xx推荐、xx哪家好"等词的时候，我们通过优化公司/业务/品牌的排名，让用户对我们感兴趣；
2、用户搜索时"xx怎么样"等词的时候，我们通过详细展示公司/业务/品牌的详细信息和优势描述，打消用户对我们的顾虑；
3、当用户搜索"xxx怎么联系"等词的时候，我们通过优化公司/业务/品牌的电话号码展现、地址展现等，为销售转化解决"最后一公里"。
（请根据本方案的实际情况，优化一下以上的描述）

第四部分，以表格形式给出GEO营销合作方案。内容如下：

优化平台：豆包+DeepSeek+文心一言+通义千问+腾讯元宝
验收平台：豆包+DeepSeek+文心一言+通义千问+腾讯元宝
服务周期：一个季度
优化范围：以上"核心业务关键词"相关词包（以词包为单位进行GEO优化，在合作期间，每个词包均可持续补充词根相似、适合做GEO优化的关键词，不限数量）
赠送服务：GEO咨询/销售转化链路策划
费用：80000元

方案下加上备注文字（分行显示、不改动）：
备注：一个季度为关键词优化正式上线的第1天开始直至第90天。

别人：只做部分关键词的GEO优化
我们：覆盖潜在客户搜索行为的全量词包+咨询/销售转化链路策划的GEO营销解决方案。

第五部分，附上以下不改动的文字：

艾奇GEO优化服务说明
1、艾奇在线所定义的GEO词包为：一条业务线上与业务相关的、词根相似的、适合做GEO优化并基本覆盖用户搜索习惯的业务关键词的集合，在GEO优化服务期间可协商补充。
2、精细化拆分关键词词包，覆盖产品类目词包（行业词包）、品牌词包、人群词包、场景词包，覆盖潜在客户的搜索习惯类别；
3、以"词包"为单位进行GEO优化，只要是与业务产品相关的、适合做GEO优化的搜索词，在合作期间均可以补充加入GEO优化服务中，尽可能覆盖潜在客户的搜索行为，合作期间每个词包实际优化关键词数量可达到300以上；
4、不同城市/区域的搜索词（如北京搬家公司vs上海搬家公司），一般需要分开优化、分开收费。

GEO优化服务合作流程
1、商定合作方案，约定验收关键词（5-12个）
2、签订合同，甲方打款
3、到款后，3-7天优化准备期及测试期
4、AI排名结果正式上线，开始排名反馈
5、每周巡检、优化并反馈情况
6、合作期满提供合作结案报告

GEO服务验收标准
1、GEO优化服务验收标准（合同条款）：甲方在验收时，可在全国范围内组织100人次在约定的各个目标平台使用包含验收关键词的词句进行搜索（搜索时须开启"新对话"，并勾选"深度思考、联网搜索"两个功能），并对搜索结果中甲方品牌/业务/产品信息的展现及排名进行统计。乙方承诺对于每个约定的验收关键词，在每个约定的目标平台的搜索结果展现并排名前五的概率达到60%。
2、GEO优化服务验收标准（合同条款）：验收流程：自GEO优化上线之日起每个月可验收一次，若有部分关键词排名下降，我们会在一天内进行优化；若验收不达标，则在 3 个工作日内调整优化，甲方 3 个工作日内组织二次验收；二次验收仍不达标，甲方有权单方解除合同，我们向甲方退还从验收不达标之日起的剩余全部费用。

方案来自：
公司名称：艾奇在线（厦门）营销咨询有限公司
联系方式：199-0601-5455

艾奇AI——深耕营销15年，与盼盼、雅客、豪士、喜多多、新东方、尚德、平安等知名品牌深度合作。同时艾奇在线也在与本地生活、商务合作、工业制造、教育培训等众多行业的多家中小企业合作中。

以上为方案模板。请开始思考并生成方案吧！（输出时不要用代码块展示关键词列表）

## 风格与纪律要求

1. 所有关键词必须是用户真实会搜的完整词句，不是抽象的产品名词
2. 关键词列表一行一个词，不用代码块展示
3. 推广文章结构要实操可落地，不是泛泛而谈
4. 转化链条描述需贴合具体业务场景，不要照搬模板措辞
5. 全文中文，专业术语可保留英文缩写
6. 方案全文输出为中文 Markdown 格式`,
};

function buildPrompt(type, params) {
  let prompt = PROMPTS[type];

  if (type === 'geo') {
    prompt = prompt.replace('{{BRAND_INFO}}', params.brand_info || '未提供');
  } else {
    prompt = prompt
      .replace('{{BRAND_NAME}}', params.brand_info || '品牌/公司名')
      .replace('{{PRODUCT_INFO}}', params.product_info || '未提供')
      .replace('{{KEYWORDS}}', params.keywords || '请自行思考总结得出');
  }

  return prompt;
}

export default async (request) => {
  if (request.method !== 'POST') {
    return new Response(JSON.stringify({ error: 'Method not allowed' }), {
      status: 405,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  let body;
  try {
    body = await request.json();
  } catch {
    return new Response(JSON.stringify({ error: '无效的请求体' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const { type, brand_info, product_info, keywords } = body;

  if (!type || !brand_info) {
    return new Response(JSON.stringify({ error: '缺少必要参数：type 和 brand_info' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  if (!['geo', 'geo-b'].includes(type)) {
    return new Response(JSON.stringify({ error: 'type 只能是 geo 或 geo-b' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  if (!process.env.ARK_API_KEY || !process.env.ARK_ENDPOINT_ID) {
    return new Response(JSON.stringify({ error: '服务端未配置 API Key 或模型接入点' }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const prompt = buildPrompt(type, { brand_info, product_info, keywords });
  const encoder = new TextEncoder();

  const stream = new ReadableStream({
    async start(controller) {
      try {
        const response = await client.chat.completions.create({
          model: process.env.ARK_ENDPOINT_ID,
          messages: [{ role: 'user', content: prompt }],
          max_tokens: 16000,
          stream: true,
        });

        for await (const chunk of response) {
          const content = chunk.choices?.[0]?.delta?.content;
          if (content) {
            controller.enqueue(encoder.encode(`data: ${JSON.stringify({ text: content })}\n\n`));
          }
        }

        controller.enqueue(encoder.encode('data: [DONE]\n\n'));
        controller.close();
      } catch (err) {
        console.error('API error:', err);
        controller.enqueue(encoder.encode(`data: ${JSON.stringify({ error: err.message })}\n\n`));
        controller.close();
      }
    },
  });

  return new Response(stream, {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
    },
  });
};

export const config = {
  path: '/api/generate',
};
